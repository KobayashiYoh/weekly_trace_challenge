import 'package:flutter/material.dart';

class ItemListView extends StatelessWidget {
  final int index;
  const ItemListView({Key? key, required this.index}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
